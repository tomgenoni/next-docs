import Props from 'pretty-proptypes';
import BlogPost from '../../../components/BlogPost';
import Tag from '../../../components/Tag';

export const meta = {
  title: 'Tag',
  description: 'Categorization of a thing',
};

export default ({ children }) => <BlogPost meta={meta}>{children}</BlogPost>;

This is an example of editing live code using `react-live` within an mdx file. Syntax highlighting provided by `prism-react-renderer`.

```js live=true
<Tag>Edit me</Tag>
```

An example of code in a standard Markdown code block.

```js
const foo = 'bar';
```

An example of our React `Tag` component.

<Tag>I’m a tag</Tag>

<Props heading='Props' props={require('!!extract-react-types-loader!../../../components/Tag')} />
